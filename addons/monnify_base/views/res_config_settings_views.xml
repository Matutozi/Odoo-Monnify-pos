<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form_monnify" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.monnify</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <!-- TODO: this xpath target is a placeholder — confirm a real
                 anchor in base.res_config_settings_view_form for Odoo 18
                 before relying on it, then build the actual Monnify
                 settings block (api key / secret / contract code / base
                 url fields + Test Connection button) per
                 docs/architecture.md section 5.2. -->
            <xpath expr="//form" position="inside">
                <!-- placeholder, not yet implemented -->
            </xpath>
        </field>
    </record>
</odoo>
