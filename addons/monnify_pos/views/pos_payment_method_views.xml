<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_pos_payment_method_form_monnify" model="ir.ui.view">
        <field name="name">pos.payment.method.form.inherit.monnify</field>
        <field name="model">pos.payment_method</field>
        <field name="inherit_id" ref="point_of_sale.pos_payment_method_view_form"/>
        <field name="arch" type="xml">
            <!-- TODO: this xpath target is a placeholder — confirm a real
                 anchor field on point_of_sale.pos_payment_method_view_form
                 for Odoo 18 (e.g. near an existing use_payment_terminal
                 field, if one exists) before relying on it. -->
            <xpath expr="//form" position="inside">
                <!-- placeholder, not yet implemented:
                     <field name="use_payment_terminal_monnify"/> -->
            </xpath>
        </field>
    </record>
</odoo>
